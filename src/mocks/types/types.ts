export interface FormData {
    email: string
    password: string
}

export interface ServerResponse {
    success: boolean
    message: string
}

export interface User {
    email: string;
    password: string;
}